package dev.gaphunter.idempotencykeycompanion.gutter

import com.intellij.icons.AllIcons
import javax.swing.Icon

/** Reuses the platform's own bundled warning icon -- same precedent as every other gutter-icon plugin in this catalog. */
object IdempotencyKeyIcons {
    val RISK: Icon = AllIcons.General.InspectionsWarningEmpty
}
